# 代碼審查報告：`75f80a2..b99ab4e`

- **日期**：2026-08-14
- **審查範圍**：`75f80a2..b99ab4e`（最近 2 個 commits）
- **審查方式**：requesting-code-review skill，由 reviewer subagent 執行唯讀審查
- **外部驗證**：已透過 https://opencode.ai/docs/plugins/ 及本機安裝版本對應的 v1.18.15 原始碼（`packages/opencode/src/config/plugin.ts`、`config.ts`、`plugin/shared.ts`）驗證 plugin API 行為

## 審查範圍確認

- `d740937` docs: 更新 `.agent_task_state.md` 與 `CHANGELOG.md`
- `b99ab4e` feat: 新增 `.opencode/plugins/graphify.js`、`opencode.json`（新檔）、`QWEN.md`（新檔）

## Strengths（值得肯定之處）

1. **Plugin hook API 用法完全正確（已逐項驗證，非臆測）**：
   - `.opencode/plugins/` 為官方文件明定的專案級 plugin 目錄（複數，會自動載入）。
   - `export const GraphifyPlugin = async ({ directory }) => ...` 簽名符合官方文件的基本結構。
   - `"tool.execute.before": async (input, output) => { if (input.tool === "bash") { output.args.command = ... } }` 與官方文件中的 shescape 範例模式完全一致。
2. **防禦性降級設計良好**：`existsSync(join(directory, "graphify-out", "graph.json"))` 守門，在無知識圖譜的環境（如全新 clone 且未跑 graphify）會靜默 no-op，不會報錯。且 `graphify-out/graph.json` 有被 git 追蹤，本 repo 內可正常觸發。
3. **檢查順序正確**：`reminded` → `existsSync` → `tool === "bash"`。非 bash 工具呼叫不會消耗 one-shot flag，graph 於 session 中途生成也仍能觸發。
4. **CHANGELOG 清理方向正確**：移除了誤植於 `[0.1.0]` 區段的 task-state 垃圾內容（`✅ 進展 / Progress` 區塊），並修正了「文件 / Documentation」項目清單被「移除 / Removed」標題從中截斷的問題。
5. **`.agent_task_state.md` 合規**：16 行（上限 50）、遵循 目標/進展/下一步 格式、更新日期 2026-04-10 與 commit 日期一致。

## Issues

### Critical (Must Fix)

無。

### Important (Should Fix)

**1. `CHANGELOG.md:22` — 「移除 / Removed」區段位置仍造成標題層級錯置**

- **問題**：修復後的結構為 `### 新增功能 / Added`（L10) → `#### 文件 / Documentation`(L12) → `### 移除 / Removed`(L22) → `#### 錯誤處理 / Error Handling`(L26) → … → `#### 權限系統 / Permission System`(L101) → `## [0.1.0]`(L107)。Markdown 標題層級下，L26–L101 共九個 H4 子章節（錯誤處理、環境壓縮、程式碼品質、設定管理、測試框架、工具系統、環境建構、TUI 增強、權限系統）**全部結構性地歸屬於「移除 / Removed」之下**，約 50 條「新增」項目在 TOC/渲染導覽中會顯示為 Removed 的子項。
- **為何重要**：此缺陷在 base 版本已存在，但本 commit 的目的即是清理 CHANGELOG，卻只修了 Documentation 清單的分組、把問題區段移到另一個仍錯誤的位置——等於清理工作只做了一半，留下的錯誤影響面更大（整批 Added 功能被誤歸類）。
- **修法**：將整個 `### 移除 / Removed` 區段（L22–L24）移至 `#### 權限系統 / Permission System` 區塊之後、`## [0.1.0]` 之前。

**2. `b99ab4e` — feat commit 未更新 `CHANGELOG.md`**

- **問題**：專案全域規範的 DoD 明定「異動後更新 CHANGELOG」，且 `[Unreleased]` 區段有在追蹤功能新增；但 graphify plugin、`opencode.json`、`QWEN.md` 三項變更均未留下任何 CHANGELOG 條目。`d740937`（docs commit）時間上早於 feat commit，無法代為記錄，補登責任在 feat commit。
- **修法**：於 `[Unreleased]` → `### 新增功能 / Added` 補一則條目，例如「**Graphify OpenCode Plugin**：新增 `.opencode/plugins/graphify.js`，於 bash 工具呼叫注入知識圖譜導引提醒」。

**3. `QWEN.md` 未登錄至 `README.md` 的 Documentation Index**

- **問題**：`README.md:113` 的自訂維護規則第 1 條明定「若新增、重新命名或刪除任何 `.md` 檔案，**必須**同步更新本節『Documentation Index / 文件索引』」。本次新增根目錄 `QWEN.md`，但 `README.md` 完全未動（`git diff --stat` 確認），Root Documents 清單（L94–L99）缺此條目。
- **為何重要**：這是專案自己立下且寫進 README 的強制規則，違反會讓索引與實際文件漂移，後續 agent 依索引導航時會漏掉 QWEN.md。
- **修法**：在 Root Documents 清單補一行 `- [QWEN.md](QWEN.md) - Graphify 知識圖譜導引規則（Qwen agent 用） / ...`。

### Minor (Nice to Have)

**4. `opencode.json:2-4` — `plugin` 陣列註冊為多餘（已驗證無害，但建議移除）**

- 依官方文件，`.opencode/plugins/` 下的檔案**啟動時自動載入**，`plugin` 陣列是給 npm 套件用的。進一步核對 v1.18.15 原始碼：`ConfigPlugin.load()` 掃描 `{plugin,plugins}/*.{ts,js}` 後與 config 宣告的 spec 一起進入 `deduplicatePluginOrigins`，兩者對本檔案解析出**相同的 `file://` URL**，會被去重——所以現況不會雙重載入、不會雙重注入，屬「冗餘但無害」。
- 但這依賴 v1 去重的實作細節；若降版或行為變更，冗餘註冊可能變成雙重注入。建議移除 `plugin` 陣列（留空 `{}` 或僅保留 `"$schema"`）。

**5. `.opencode/plugins/graphify.js:14-17` — 命令字串改寫的副作用未記錄**

- 前置注入 `echo "..." && ` 會改變 opencode 權限系統/allowlist 所看到的命令字串（例如針對 `git status` 的精確比對規則，實際看到的會是 `echo ... && git status`），也會讓 transcript 中的第一條 bash 命令與使用者審核畫面出現注入前綴。功能上可運作，但屬應記錄的已知取捨。
- 另外 `reminded` 為 plugin 實例層級的閉包變數，在同一 opencode server process 的多個 session 間只會提醒一次（process 級而非 session 級）。若這是有意的，建議在檔頭註解一句話說明。

**6. Commit 主旨語言偏離全域規範**

- 全域規範要求主旨與內容均為繁體中文，兩個 commit 主旨皆為英文。查 `git log` 近期歷史確實中英混雜（緊鄰的 `75f80a2` 亦為英文，但 `7579bbc`~`10298b0` 一連串皆為繁體中文），故列為 Minor；建議後續回到繁體中文主旨以符合明文規範。

**7. Plugin 無任何驗證留痕**

- 22 行的 glue code 不需正式測試框架，但建議在 commit message 或 CHANGELOG 補一句手動驗證方式（例如「啟動 opencode 後執行任一 bash 命令，輸出開頭應出現 `[graphify]` 提醒」），讓後人可快速確認 plugin 仍被載入。

## Recommendations

1. 一次性補齊文件債：CHANGELOG 結構修正 + 補登 plugin 條目 + README 索引補 QWEN.md，可合併為一個 `docs` commit 完成。
2. `opencode.json` 移除冗餘的 `plugin` 註冊，並順勢提交工作區中那筆 `$schema` 新增（可獲得編輯器 schema 校驗）。
3. 在 `graphify.js` 檔頭註解補兩行設計取捨說明（一次性注入、命令改寫副作用），讓此 plugin 的維護者不需重新考據。

## Assessment

**Ready to merge?** With fixes

**Reasoning:** 核心交付物（graphify plugin）經官方文件與 v1.18.15 原始碼雙重驗證，hook API 用法正確、降級行為健全，功能面無缺陷；但文件層面有三項應修項目——CHANGELOG 標題層級錯置（清理只做一半）、feat 漏登 CHANGELOG、QWEN.md 漏登 README 索引，皆為專案自訂規範的明確要求，修正成本低，補齊後即可合併。

---

> 備註：使用者已決定本次僅留存報告，不進行修復（2026-08-14）。
